import { assert } from "console";
import { findKeywordInPDF } from "../handler"


describe("This is a findKeywordInPDF test", () => {
    test("Check the keywords are returning", async() => {
        assert(1===1, 'equals')                
    })
})
